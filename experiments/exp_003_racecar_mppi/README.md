# Experiment 003: Racecar JAX MPPI

This folder is reserved for Task 003:

```text
docs/tasks/003_racecar_jax_mppi.md
```

Students may place racecar-specific benchmark code, plotting utilities, configuration files, and result summaries here. Runnable entrypoints should go in `examples/`. Reusable racecar MPPI and Smooth MPPI controller code should go in `src/controllers/mpc/`.

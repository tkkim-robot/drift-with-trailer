# Experiment 001: MPC Baseline

This folder is reserved for the first student check-in task:

```text
docs/tasks/001_nonlinear_mpc_cartpole_swingup.md
```

Students may place cartpole-specific dynamics, plotting utilities, and configuration files here. Runnable entrypoints should go in `examples/`. Reusable MPC controller code should go in `src/controllers/mpc/`.

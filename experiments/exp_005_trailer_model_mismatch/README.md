# Experiment 005: Trailer Model Mismatch

This folder is reserved for Task 005:

```text
docs/tasks/005_trailer_model_mismatch_mppi.md
```

Students may place benchmark code, plotting utilities, configuration files, linked media notes, and result summaries here. Runnable entrypoints should go in `examples/`. Reusable dynamics, controller, cost, and model-selection code should go in `src/`.

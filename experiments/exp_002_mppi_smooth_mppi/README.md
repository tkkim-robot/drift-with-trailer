# Experiment 002: MPPI and Smooth MPPI

This folder is reserved for Task 002:

```text
docs/tasks/002_mppi_smooth_mppi_cartpole.md
```

Students may place cartpole-specific benchmark code, plotting utilities, configuration files, and result summaries here. Runnable entrypoints should go in `examples/`. Reusable MPPI and Smooth MPPI controller code should go in `src/controllers/mpc/`.

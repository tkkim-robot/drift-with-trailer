# Experiment 007: Vehicle Residual Dynamics Learning

This folder is reserved for Task 007:

```text
docs/tasks/007_vehicle_residual_dynamics_mppi.md
```

Students may place dataset collection scripts, training utilities, plotting utilities, configuration files, linked media notes, and result summaries here. Runnable entrypoints should go in `examples/`. Reusable dataset transforms, learned residual models, and MPPI rollout adapters should go in `src/`.

# Experiment 006: Online Learned CartPole Dynamics

This folder is reserved for Task 006:

```text
docs/tasks/006_online_learned_cartpole_mppi.md
```

Students may place online-learning experiment code, plotting utilities, dataset notes, configuration files, and result summaries here. Runnable entrypoints should go in `examples/`. Reusable learned dynamics models, dataset buffers, and controller adapters should go in `src/`.
